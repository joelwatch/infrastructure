#local .terraform directories

**/.terraform/*

#.tfstate files

*.tfstate
*.tfstate.*

#crash log files

crash.log
crash.*.log

#Exclude all .tfvars files, which are likely to contain sensitive data, such as passwd, private key
*.tsvars.json

#igniore overide files 
overide.tf
overide.tf.json
*_overide.tf
*_overide.tf.json

#igniore transient lock info
.terraform.tfstate.lock.info
.terraform.lock.hcl

#igniore cli config
.terraformrc
terraform.rc